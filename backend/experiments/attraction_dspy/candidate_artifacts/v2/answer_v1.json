{
  "generate": {
    "traces": [],
    "train": [],
    "demos": [],
    "signature": {
      "instructions": "Generate a structured JSON response that includes recommendations for tourist locations based on the following inputs: the language of the output, the user's question about recommendations, selected candidates in JSON format, and the reasons for their selection also in JSON format. Ensure the output includes keys for language, recommendations, and no_result_reason, with the recommendations being a list of up to 3 items, each containing place_id, name, recommendation_reason, description_evidence, review_evidence, congestion, and weather information.",
      "fields": [
        {
          "prefix": "Language:",
          "description": "${language}"
        },
        {
          "prefix": "Question:",
          "description": "${question}"
        },
        {
          "prefix": "Selected Candidates Json:",
          "description": "${selected_candidates_json}"
        },
        {
          "prefix": "Selection Reasons Json:",
          "description": "${selection_reasons_json}"
        },
        {
          "prefix": "Structured Answer Json:",
          "description": "A valid JSON object string only, with exactly language, recommendations, and no_result_reason keys. recommendations is a list (at most 3) whose items contain place_id, name, recommendation_reason, description_evidence, review_evidence, congestion, weather, and optional visitor_note. Each congestion/weather object must have status available|unavailable; unavailable must contain no value/basis/observed_at. Use only selected candidate evidence. Do not return prose, Markdown, or another JSON shape."
        }
      ]
    },
    "lm": null
  },
  "metadata": {
    "dependency_versions": {
      "python": "3.12",
      "dspy": "3.2.1",
      "cloudpickle": "3.1"
    }
  }
}
